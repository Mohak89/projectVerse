import styled from "styled-components";

const NotFoundDiv = styled.div`
    margin-left: auto;
    margin-right: auto;
    width: fit-content;
    display: flex;
    flex-direction: column;
    padding: 2rem;
    text-align: center;
`

const ErrorCode = styled.span`
    font-size: 95px;
    color: grey;
`
export {ErrorCode,NotFoundDiv}

